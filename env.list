# nema-mar-portal local development configuration
# Copy to env.local and edit as needed, then run:
#   ./run_local.sh

# --- PostgreSQL ---
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fastschema
DB_USER=fastschema
DB_PASS=fastschema

# --- FastSchema sidecar ---
FS_PORT=8000
FS_APP_KEY=local_dev_key_32_characters_ok_
FS_ADMIN_USER=admin
FS_ADMIN_PASS=admin123

# --- S3 object store (leave STORAGE_BUCKET empty to use local disk) ---
STORAGE_BUCKET=test-geonet-archive
STORAGE_REGION=ap-southeast-2

# --- nema-mar-app ---
APP_PORT=8080
FASTSCHEMA_URL=http://localhost:8000

# --- SMTP (optional — email sending is skipped if not configured) ---
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_RECIPIENTS=

# --- Observability (optional) ---
DDOG_API_KEY=
